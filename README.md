1) create the virtual environemt with python version 3.12 to install necessary dependencies
2) Install all the dependencies through requirements.txt file
3) .ipynb file contains the entire code for the multimodal rag in cell froamte and src folder contains the modular code for the multimodal rag
4) run the python gradio_app.py to open the interactive interface to query on the indexed data.

# Mutimodal-RAG

## Overview
Multimodal-RAG is a repository designed for utilizing multimodal retrieval-augmented generation (RAG) techniques to query complex layout unstructured data.
